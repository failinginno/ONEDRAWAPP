/**
 * Compatibility facade for the existing UI. New code should import domain
 * models from `domain/models` and `useOnedraw` from `providers/OnedrawContext`.
 */
export * from '../providers/demo/DemoProvider';
